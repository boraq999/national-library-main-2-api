import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nationallibrary.app',
  appName: 'national-library-app',
  webDir: 'build'
};

export default config;
